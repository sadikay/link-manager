require 'spec_helper'

describe Link::Manager do
  it 'has a version number' do
    expect(Link::Manager::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
